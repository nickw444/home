nick_bedtime:
  # Turns on the lifx bedside lamp, turns off the downlights + fairy lights, then fade to
  # darkness over 5 minutes.
  # Handled by node-red
  alias: Bedtime
  sequence:
    - event: bedtime_start
