language: python

python:
  - "3.7"

jobs:
  include:
    - stage: test
      name: '[ha/313a] check config'
      script:
        ./home-assistant/tools/test_config.sh 313a
    - stage: test
      name: '[ha/martin-pl] check config'
      script:
        ./home-assistant/tools/test_config.sh martin-pl
